# Многопоточный анализатор текстовых файлов

## Описание проекта

Консольное приложение на C# для многопоточного анализа текстовых файлов. Программа параллельно обрабатывает несколько файлов, подсчитывая количество слов и символов в каждом, с использованием современных возможностей C#: структур данных, многопоточности, асинхронного программирования и механизмов синхронизации.

## Функциональность

- **Многопоточная обработка** нескольких файлов одновременно
- **Асинхронное чтение** файлов с использованием `async/await`
- **Подсчет статистики** для каждого файла:
  - Количество слов
  - Количество символов (без учета пробелов)
- **Синхронизация доступа** к разделяемым данным с помощью мьютексов
- **Агрегация результатов** с выводом общего итога
- **Обработка ошибок** (файл не найден, нет доступа и т.д.)
- **Автоматическое тестирование** основных функций

## Архитектура

### Основные компоненты

1. **`FileAnalysis`** - структура для хранения результатов анализа
   - Неизменяемая (`readonly`)
   - Содержит имя файла, количество слов и символов

2. **`AggregateResult`** - класс для агрегации общих результатов
   - Потокобезопасное накопление данных
   - Использование `Mutex` для синхронизации

3. **`Program`** - основной класс приложения
   - Управление многопоточной обработкой
   - Асинхронные операции ввода-вывода
   - Тестирование функциональности

## Требования

- .NET 6.0 или выше
- ОС: Windows, Linux или macOS

## 🚀 Использование

### Запуск с указанием файлов

```bash
dotnet run -- file1.txt file2.txt file3.txt
```

или

```bash
MultiThreadFileAnalyzer.exe file1.txt file2.txt file3.txt
```

### Запуск без параметров

Если не указаны файлы, программа автоматически найдет все `.txt` файлы в текущей директории:

```bash
dotnet run
```

## Пример вывода

```
=== Многопоточный анализатор текстовых файлов ===

Найдено файлов для анализа: 3

Начало обработки файлов...

[4] Обработка файла: document1.txt
[5] Обработка файла: document2.txt
[6] Обработка файла: document3.txt
[4] Завершена обработка: document1.txt
[5] Завершена обработка: document2.txt
[6] Завершена обработка: document3.txt

Обработка всех файлов завершена.

=== РЕЗУЛЬТАТЫ АНАЛИЗА ===

1. document1.txt: 1250 слов, 8340 символов
2. document2.txt: 890 слов, 5670 символов
3. document3.txt: 2100 слов, 14250 символов

ИТОГ: 4240 слов, 28260 символов.
```

## Детали реализации

### Многопоточность

```csharp
// Параллельная обработка файлов
var tasks = filePaths.Select(ProcessSingleFileAsync);
await Task.WhenAll(tasks);
```

### Синхронизация с мьютексами

```csharp
_mutex.WaitOne();
try
{
    // Критическая секция
    _totalWords += result.WordCount;
    _totalChars += result.CharCount;
}
finally
{
    _mutex.ReleaseMutex();
}
```

### Асинхронное чтение

```csharp
static async Task<string> ReadFileContentAsync(string filePath)
{
    using (var reader = new StreamReader(filePath))
    {
        return await reader.ReadToEndAsync();
    }
}
```

## Тестирование

Программа включает автоматические тесты:

1. **Создание тестовых файлов** с известным содержимым
2. **Проверка подсчета слов и символов**
3. **Тестирование обработки ошибок** (несуществующие файлы)
4. **Очистка тестовых данных** после выполнения

Запуск тестов происходит автоматически после основной обработки.

## Обработка ошибок

Приложение корректно обрабатывает следующие ситуации:

- Файл не найден
- Отсутствие прав доступа к файлу
- Ошибки чтения файла
- Пустые файлы
- Некорректные пути

## Производительность

- **Масштабируемость**: количество одновременно обрабатываемых файлов ограничено только ресурсами системы
- **Эффективность**: асинхронные операции не блокируют потоки
- **Безопасность**: мьютексы предотвращают состояние гонки